<!DOCTYPE html>
<html lang="en">
<head>
  <title>Login</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <style>
    body {
      background-image: url('/images/background.webp');
      font-family: 'Montserrat', sans-serif;
      margin: 0;
      padding: 0;
      height: 100%;
      background-size:cover;
      overflow:hidden;
    }

    .login-container {
      background: white;
      border-radius: 1rem;
      box-shadow: rgba(50, 50, 93, 0.25) 0px 13px 27px -5px, rgba(0, 0, 0, 0.3) 0px 8px 16px -8px;
      display: flex;
      flex-direction: column;
      align-items: center;/*ChatGPT for flex direction and alignment*/
      justify-content: center;
      padding: 2rem;
      margin-top: 3rem; 
      max-width: 400px; 
      margin-left: auto;
      margin-right: auto;
      top: 100%;
      position: relative;
      transition: all .3s ease-out;
      height: 100%;
      animation: loginLoad 1000ms ease-in-out;
    }
    
   
    @keyframes loginLoad{ /*animation for loading in*/
        0% {   
            transform: translateY(+100%);
        }

        100% {
            transform: translateY(0);
        }
    }

    h1 {
      margin-top: 0;
      margin-bottom: 1.5rem; 
      font-family:'Trebuchet MS';
    }

    .form-row {
      position: relative;
      margin-bottom: 1rem; 
    }

    .form-input {
      box-sizing: border-box;
      padding: 1rem;
      border: 0;
      border-bottom: 1px solid lightgrey;
      width: 100%;
      font-size: 1rem; 
    }

    .form-input::placeholder {
      color: transparent;
      display: none; /*Animation and CSS for login textbars provided from ChatGPT*/
    }

    .form-input:focus {
      outline: none;
      border-bottom: 2px solid #129606;
    }

    .form-label {
      position: absolute;
      color: gray;
      bottom: 0.5rem;
      left: 0;
      transition: opacity .1s ease-in-out, transform .1s ease-in-out; 
    }

    .form-input:focus ~ .form-label,
    .form-input:not(:placeholder-shown) ~ .form-label {
      opacity: .65;
      transform: scale(.85) translateY(-1.3rem); /*Animation CSS for login textbars provided from ChatGPT*/
    }

    .forgot-pwd {
      float: right;
      text-decoration: none;
      color: #129606;
      margin-top: 1rem;
    }

    .submit-btn {
      margin-top: 2rem;
      width: 100%;
      font-size: 1.2rem;
      border-radius: 2rem;
      padding: 0.5rem 2rem;
      background: #129606;
      color: white;
      border: 0;
    }

    .submit-btn:hover {
      cursor: pointer;
      background: #0d7e03; 
      text-decoration: none;
    }

    .sign-up-text {
      color: gray;
      display: block;
      margin-top: 2rem;
      text-align: center; 
    }

    .sign-up-text > a {
      text-decoration: none;
      color: #129606;
    }

    .nav-link{
        font: weight 2px;
        color:ghostwhite
    }

    .navbar-brand {
            font-family:'Trebuchet MS';
    }
  </style>
</head>
<body>

<nav class="navbar navbar-expand-md bg-success navbar-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">SuperFresh</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="collapsibleNavbar">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="/">About</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/signup">Sign Up</a>
        </li>
      </ul>
    </div>
  </div>
</nav>

<div class="login-container">

  <form action="/login" method="POST">
    <img src="/images/SuperFresh.png" alt="SuperFresh icon" height="50" width="200"class="d-block w-100">
    <br>
    <div class="form-row">
      <input type="email" id="email" name="email" class="form-input" placeholder="example@email.com" required>
      <label for="email" class="form-label">Email</label>
    </div>
    <div class="form-row">
      <input type="password" id="password" name="password" class="form-input" placeholder="pwd" required>
      <label for="password" class="form-label">Password</label>
    </div>
    <a href="/forgotpass" class="forgot-pwd">Forgot password?</a>
    <button type="submit" class="submit-btn">Login</button>
  </form>
  

  <p class="sign-up-text">Don't have an account? <a href="/signup">Sign Up</a></p>
</div>


 <div class="modal fade" id="loginFailedModal" tabindex="-1" aria-labelledby="loginFailedModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="loginFailedModalLabel">Login Failed</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <p>Invalid username or password. Please try again.</p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-success" data-bs-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>

<script>
  // JavaScript to show modal if login failed
  const urlParams = new URLSearchParams(window.location.search);
  const loginFailed = urlParams.get('loginFailed');

  if (loginFailed === 'true') {
    var myModal = new bootstrap.Modal(document.getElementById('loginFailedModal'));
    myModal.show();
  }
</script>
</body>
</html>
