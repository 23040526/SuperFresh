<!DOCTYPE html>
<html lang="en">
<head>
  <title>Home</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <style>
    .Invcontainer {
      background: white;
      border-radius: 1rem;
      box-shadow: rgba(50, 50, 93, 0.25) 0px 13px 27px -5px, rgba(0, 0, 0, 0.3) 0px 8px 16px -8px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 2rem;
      margin-top: 3rem; 
      max-width: 800px; 
      margin-left: auto;
      margin-right: auto;
      top: 100%;
      position: relative;
      transition: all .3s ease-out;
      height: 100%;
    }

    .Icon {
      margin-left: 2rem;
      margin-right: auto;
    }
    
    h2 {
      margin-top: -2.5rem;
      margin-left: 2rem;
      margin-right: auto;
      color: #92745B;
      font-family: 'Trebuchet MS';
      font-weight: bold;
    }

    body {
      background-image: url('/images/background.webp'); 
      background-size: cover;
      background-repeat: no-repeat;
    }

    .navbar-brand {
      font-family: 'Trebuchet MS';
    }

    .nav-link {
      font-weight: 2px;
      color: ghostwhite;
    }

    .carousel-caption {
      color: white;
      backdrop-filter: blur(2px) brightness(0.8);
      border-radius: 20px;
    }
    
    nav {
      background-color: #92745B;
    }
  </style>
</head>
<body>
  <%- include ('partials/staffnav.ejs') %>
  <div class="Invcontainer">
    <img src="/images/SuperInv.png" alt="SuperInv icon" height="40" width="160" class="Icon">
    <div class="text-center"><h2>Inventory</h2></div>
    <br>
  
    <table class="table table-hover small text-center">
      <thead>
        <tr>
          <th width="50">ID</th>
          <th width="100">Product Name</th>
          <th width="100">Product Image</th>
          <th width="50">Type</th>
          <th width="50">Price</th>
          <th width="50">Quantity</th>
          <th width="50">Edit</th>
          <th width="50">Delete</th>
        </tr>
      </thead>
      <tbody>
        <% for (let i = 0; i < products.length; i++) { %>
          <tr>
            <td><%= products[i].id %></td>
            <td><a href="/InvInfo/<%= products[i].id %>"><%= products[i].name %></a></td>
            <td><img src="/images/<%= products[i].image %>" width="40%"></td>
            <td><%= products[i].type %></td>
            <td>$<%= products[i].price %></td>
            <td><%= products[i].quantity %></td>
            <td><a href="/editProduct/<%=products[i].id %>" class="btn btn-success">Edit</a></td>
            <td><a href="/deleteProduct/<%=products[i].id %>" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this product?')">Delete</a></td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </div>
</body>
</html>
